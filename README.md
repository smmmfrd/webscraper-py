# Webscraper-py

A boot.dev project

## Commands

### Run Tests

`uv run -m unittest`

### Run program

`uv run main.py BASE_URL`

*For instance: * `uv run main.py https://learnwebscraping.dev/practice/ecommerce`